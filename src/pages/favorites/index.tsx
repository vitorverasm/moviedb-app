import React from 'react';
import {Container, Text} from '../../styles';

const Favorites = () => {
  return (
    <Container centered>
      <Text>Favorites</Text>
    </Container>
  );
};

export default Favorites;
